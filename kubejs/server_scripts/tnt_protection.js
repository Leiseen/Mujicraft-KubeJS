// TNT 和苦力怕防爆保护
// 阻止所有爆炸破坏方块，但保留爆炸的视觉和音效

LevelEvents.beforeExplosion(event => {
    // 取消原始爆炸（包括方块破坏和伤害）
    event.cancel();
    
    // 创建一个新的爆炸，只有视觉和音效，不破坏方块也不造成伤害
    const explosion = event.level.createExplosion(event.x, event.y, event.z);
    
    // 设置爆炸强度（影响视觉效果的大小）
    explosion.strength(event.radius || 3.0);
    
    // 不破坏地形
    explosion.damagesTerrain(false);
    
    // 不引起火焰
    explosion.causesFire(false);
    
    // 保留爆炸源信息（用于粒子效果）
    if (event.exploder) {
        explosion.exploder(event.exploder);
    }
    
    // 触发爆炸
    explosion.explode();
});

console.info('═══════════════════════════════════════════════════════');
console.info('💥 TNT 和苦力怕防爆保护系统 V1.0');
console.info('🛡️  功能: 阻止所有爆炸破坏方块');
console.info('🎆  效果: 保留爆炸视觉和音效');
console.info('🚫  限制: 不造成伤害和击退');
console.info('✅  状态: 已成功加载');
console.info('═══════════════════════════════════════════════════════');
